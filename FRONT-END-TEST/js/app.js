$(function	(){
	//array filled with objects with product info 
	 var products = [],

	//the filters object will hold an array of values for each filter

		// E.G:
		// filters = {
		// 		"manufacturer" = ["Apple","Sony"],
		//		"storage" = [16]
		//	}

		filters = {};

	//event handlers to help with frontend navigation

	var checkboxes = $('.entire-products input[type=checkbox]');

});